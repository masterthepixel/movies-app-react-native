import {extendTheme} from 'native-base';

export const theme = extendTheme({
  colors: {
    dark: {
      50: '#212121',
    },
    gray: {
      50: '#746A64',
      100: '#757575',
    },
    light: {
      100: '#FAFAFA',
      300: '#DEDEDE',
    },
  },
});
